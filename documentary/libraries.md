# Libraries

> - Get them from "pip freeze"
> - Install the with "pip install"

## Get libraries

```cmd
python -m pip freeze > requires.txt
```

## Install libraries

```cmd
python -m pip install -r requires.txt
```